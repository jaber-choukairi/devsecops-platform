apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: devsecops
  labels:
    app: rag
spec:
  selector:
    app: rag
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP